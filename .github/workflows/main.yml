name: .NET CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Restore
        run: dotnet restore ./EmailSchedulerApp/EmailSchedulerApp.csproj

      - name: Build
        run: dotnet build ./EmailSchedulerApp/EmailSchedulerApp.csproj --no-restore

      - name: Test
        run: dotnet test ./EmailSchedulerApp/EmailSchedulerApp.csproj --no-build --verbosity normal
