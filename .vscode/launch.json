{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "EmailSchedulerApp",
      "type": "coreclr",
      "request": "launch",

      "program": "dotnet",
      "args": ["run"],

      "cwd": "${workspaceFolder}/EmailSchedulerApp",

      "console": "integratedTerminal",

      "env": {
        "ASPNETCORE_ENVIRONMENT": "Development"
      }
    }
  ]
}